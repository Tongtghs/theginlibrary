{
  "name": "theginlibrary",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The Gin Library — site, booking, admin dashboard.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "netlify dev",
    "stripe:listen": "stripe listen --forward-to localhost:8888/.netlify/functions/stripe-webhook"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "resend": "^4.0.0",
    "stripe": "^17.0.0"
  }
}
